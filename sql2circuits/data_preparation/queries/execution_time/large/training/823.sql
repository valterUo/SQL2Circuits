SELECT MIN(mc.note) AS production_note FROM complete_cast AS cc, movie_link AS ml, movie_companies AS mc WHERE mc.note LIKE '%(co-production)%' AND ml.movie_id = mc.movie_id AND mc.movie_id = cc.movie_id;