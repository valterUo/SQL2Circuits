SELECT MIN(mc.note) AS production_note FROM company_type AS ct, movie_link AS ml, movie_companies AS mc WHERE ct.kind = 'production companies' AND ct.id = mc.company_type_id AND ml.linked_movie_id = mc.movie_id;