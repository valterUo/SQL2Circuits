SELECT MIN(mc.note) AS production_note FROM company_type AS ct, movie_companies AS mc, title AS t WHERE mc.note NOT LIKE '%(as Metro-Goldwyn-Mayer Pictures)%' AND t.id = mc.movie_id AND ct.id = mc.company_type_id;