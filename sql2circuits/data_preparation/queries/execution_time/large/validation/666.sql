SELECT MIN(mc.note) AS production_note FROM company_type AS ct, movie_link AS ml, movie_companies AS mc WHERE mc.note LIKE '%(co-production)%' AND ml.movie_id = mc.movie_id AND ct.id = mc.company_type_id;