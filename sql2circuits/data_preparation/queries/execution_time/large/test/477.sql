SELECT MIN(mc.note) AS production_note FROM company_name AS cn, movie_companies AS mc, title AS t WHERE mc.note NOT LIKE '%(as Metro-Goldwyn-Mayer Pictures)%' AND cn.id = mc.company_id AND t.id = mc.movie_id;