SELECT MIN(mc.note) AS production_note FROM company_name AS cn, movie_link AS ml, movie_companies AS mc WHERE mc.note LIKE '%(presents)%' AND ml.linked_movie_id = mc.movie_id AND cn.id = mc.company_id;