SELECT MIN(mc.note) AS production_note FROM company_type AS ct, cast_info AS ci, movie_companies AS mc WHERE mc.note LIKE '%(co-production)%' AND ci.movie_id = mc.movie_id AND ct.id = mc.company_type_id;