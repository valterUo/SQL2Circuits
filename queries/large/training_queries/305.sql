SELECT MIN(an.name) AS actress_pseudonym FROM aka_name AS an, cast_info AS ci, person_info AS pi WHERE ci.note ='(voice: English version)' AND an.person_id = ci.person_id AND pi.person_id = an.person_id;