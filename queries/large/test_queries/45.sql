SELECT MIN(mc.note) AS production_note FROM aka_title AS at, movie_companies AS mc, company_type AS ct WHERE mc.note LIKE '%(co-production)%' AND mc.movie_id = at.movie_id AND ct.id = mc.company_type_id;