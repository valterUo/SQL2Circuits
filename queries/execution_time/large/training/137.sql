SELECT MIN(mc.note) AS production_note FROM aka_title AS at, movie_link AS ml, movie_companies AS mc WHERE mc.note LIKE '%(co-production)%' AND mc.movie_id = at.movie_id AND ml.movie_id = mc.movie_id;