SELECT MIN(mc.note) AS production_note FROM company_name AS cn, aka_title AS at, movie_companies AS mc WHERE mc.note NOT LIKE '%(as Metro-Goldwyn-Mayer Pictures)%' AND cn.id = mc.company_id AND mc.movie_id = at.movie_id;