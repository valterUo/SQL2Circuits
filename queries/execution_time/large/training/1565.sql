SELECT MIN(mc.note) AS production_note FROM movie_link AS ml, cast_info AS ci, movie_companies AS mc WHERE mc.note LIKE '%(co-production)%' AND ml.movie_id = mc.movie_id AND ci.movie_id = mc.movie_id;