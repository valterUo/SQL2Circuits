SELECT MIN(mc.note) AS production_note FROM complete_cast AS cc, aka_title AS at, movie_companies AS mc WHERE mc.note LIKE '%(co-production)%' AND mc.movie_id = cc.movie_id AND mc.movie_id = at.movie_id;